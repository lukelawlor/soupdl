/*
 * hud.h contains functions for drawing parts of the game's HUD
 */

#ifndef	HUD_H
#define	HUD_H

// Initializes variables needed to draw parts of the HUD
int hud_init(void);

// Draws every component of the HUD
void hud_draw_all(void);

#endif
